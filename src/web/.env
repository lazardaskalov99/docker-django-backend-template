# ================================
# DEPRECATED: This file is no longer used
# ================================
# Please use the .env file in the root directory instead.
# All services (Django, Celery, Nginx) now use the centralized
# .env file at the project root for consistency.
#
# To migrate: Copy your custom values from this file to the
# root .env file and then you can safely delete this file.
# ================================

# Database Configuration
DB_NAME=your_database_names
DB_USER=your_database_user
DB_PASSWORD=your_secure_password
DB_HOST=db
DB_PORT=5432

# Django Settings
DJANGO_SECRET_KEY=your_super_secret_key_here
DJANGO_DEBUG=True
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1

# Redis Configuration
REDIS_HOST=redis
REDIS_PORT=6379


DJANGO_CSRF_TRUSTED=https://localhost,https://127.0.0.1,http://127.0.0.1
